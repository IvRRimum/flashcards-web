<%= form_with(model: flashcard_category) do |form| %>
  <% if flashcard_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(flashcard_category.errors.count, "error") %> prohibited this flashcard_category from being saved:</h2>

      <ul>
        <% flashcard_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div>
    <%= form.label :note, style: "display: block" %>
    <%= form.text_area :note, class: "form-control" %>
  </div>

  <div>
    <%= form.submit "Save", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
